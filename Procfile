# Heroku Procfile
# Defines process types for the application

# Web process - serves the dashboard UI and API
web: node api/server.js

# Worker process - runs duplicate detection pipeline (optional)
# Uncomment to run background jobs
# worker: node pipelines/duplicate-detection-pipeline.js

# Release phase - runs before deployment (migrations, setup, etc.)
# release: npm run verify
