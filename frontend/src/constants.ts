export const CAPACITY = {
  /** Threshold (0–1) above which capacity is considered high/warning */
  HIGH_THRESHOLD: 0.8,
  /** High threshold as a percentage (0–100) */
  HIGH_THRESHOLD_PERCENT: 80,
  /** Maximum bar percentage */
  MAX_PERCENT: 100,
} as const;
